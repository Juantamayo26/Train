#include <bits/stdc++.h>

using namespace std;

#define endl '\n'
#define ll long long
const int maxi = 2e5+10;

int main(){
  ios::sync_with_stdio(0); cin.tie(0); 
  int n;
  cin>>n;
  int maxi=0;
  for(int i=0; i<n; i++){
    int aux; cin>>aux;
    n.push_back(aux);
    maxi = max(aux, maxi);
  }
  criba[maxi];
  for(int i=0;i<n; i++){
  }
  reverse(n.begin(), n.end());
  for(int i: n){

  }
  
}

